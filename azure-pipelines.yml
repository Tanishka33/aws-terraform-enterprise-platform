trigger:
- main

pool:
  name: Default

steps:

- checkout: self

- script: |
    terraform version
  displayName: "Terraform Version"

- script: |
    cd environments/dev
    terraform fmt -check -recursive ../..
  displayName: "Terraform Format Check"